  <br>
  <hr>
  <br>
  <div class="row">
    <% if signed_in? %>
      <div class="col-md-4 col-md-offset-4">
        <%= link_to "New Game", game_path, { :class => "btn btn-and btn-lg center-block" } %>
      </div>
    <% else %>
      <div class="col-md-4 col-md-offset-4">
        <%= link_to "Create an Account", signup_path, { :class => "btn btn-and btn-lg center-block" } %>
        <h4 class="stamp text-center">or</h4>
        <%= link_to "Play Without Logging In", game_path, { :class => "btn btn-and btn-lg center-block" } %>
      </div>
    <% end %>
  </div>
  <br>